{
  "permissions": {
    "allow": [
      "Shell(which pandoc 2>/dev/null; which python3 2>/dev/null; pip3 list 2>/dev/null | grep -i docx)",
      "Shell(python3 -c  import docx doc = docx.Document('nikhilproject23.docx') for i, para in enumerate(doc.paragraphs): style = para.style.name if para.style else 'None' print(f'[{i}][{style}] {para.text[:200]}')  2 >& 1)",
      "Shell(python3 -c  import docx doc = docx.Document('nikhilproject23.docx') total = len(doc.paragraphs) for i, para in enumerate(doc.paragraphs): style = para.style.name if para.style else 'None' print(f'[{i}][{style}] {para.text[:200]}')  2 >& 1)"
    ],
    "deny": [],
    "defaultMode": "default"
  }
}